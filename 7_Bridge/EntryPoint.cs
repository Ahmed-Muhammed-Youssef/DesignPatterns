using System;

namespace _7_Bridge
{
/* 
* Category: Structural Pattern
* Definition: 
* A mechanism that decouples an interface (hierarchy) from an implementation (hierarchy). 
* used sometimes with crossplatform application as the implementation differs depending on 
* the os running the application.
*/
    class EntryPoint
    {
        static void Main(string[] args)
        {
            // BridgeExample.Test();
        }
    }
}
